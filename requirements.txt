# Outlook-Kommunikationsanalyse -- Abhaengigkeiten
# Installation:  pip install -r requirements.txt

# start.bat richtet alles selbst ein -- Python inbegriffen, falls es fehlt.
# Diese Datei wird von dort aufgerufen; von Hand braucht man sie nur, wenn die
# automatische Einrichtung am Firmenproxy scheitert.
#
# --- Pflicht -------------------------------------------------------------
# Keine.  Auswertung, Oberflaeche, HTML-Report und Zusammenfuehrung laufen mit
# der Standardbibliothek von Python 3.11 oder neuer.  Die Oberflaeche nutzt
# tkinter, das unter Windows zum Lieferumfang von Python gehoert.  Unter Linux
# ist dafuer je nach Distribution das Paket python3-tk noetig.

# --- Windows/Outlook -----------------------------------------------------
# Nur fuer das Auslesen des Postfachs noetig.  Ohne pywin32 laeuft alles
# Uebrige unveraendert -- die Auswertung rechnet dann auf der bereits
# erzeugten Zwischendatei ('okoa neu') oder auf Beispieldaten ('okoa demo').
pywin32>=306; sys_platform == "win32"

# --- Optional ------------------------------------------------------------
# Erzeugt die Zuordnungsdateien als .xlsx statt .csv, mit Auswahllisten und
# fixierter Kopfzeile.  Ohne openpyxl werden .csv-Dateien geschrieben, die
# sich ebenso in Excel oeffnen und pflegen lassen.
openpyxl>=3.1
